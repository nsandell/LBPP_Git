package util;

//TODO new command environment
public class CommandEnvironment {
	/*
	public static @interface CEInterface {
		String type();
	}
	
	public static @interface CECommand {
		String name();
		String help();
	}
	
	public CommandEnvironment(BufferedReader reader)
	{
		this.reader = reader;
	}

	public CommandEnvironment(BufferedReader reader, Object rootObject)
	{
		this.reader = reader;
		this.rootObject = rootObject;
	}
	
	public void go()
	{
	}

	private BufferedReader reader;
	private Object rootObject = null;
	private HashMap<String,Object> objs = new HashMap<String, Object>();
	*/
}
